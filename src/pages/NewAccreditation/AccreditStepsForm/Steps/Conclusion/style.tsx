import { SxProps } from '@mui/material';

const useStyles = () => {

    const containerConclusion: SxProps = {
        height: '600px',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        textAlign: 'center'
    }
    

    return { containerConclusion }
}

export default useStyles